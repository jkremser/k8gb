[build]
  command = "git remote add origin git@github.com:k8gb-io/k8gb.git && git fetch origin gh-pages && git checkout origin/gh-pages && make build"
  publish = "_site/"

[context.deploy-preview.environment]
  ACCESS_TOKEN = "not so secret"
